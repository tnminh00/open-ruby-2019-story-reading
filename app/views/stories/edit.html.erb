<% provide :button_text, t(".update") %>
<div class="breadcrumb">
  <div class="container">
    <%= link_to t(".management"), management_path, class: "breadcrumb-item" %>
    <span class="breadcrumb-item active"><%= @story.name %> (<%= t ".edit" %>)</span>
  </div>
</div>
<section class="product-sec">
  <div class="container">
    <h1><%= t ".title" %>: <%= @story.name %></h1>
    <div class="ui form col-md-offset-1 col-md-10">
      <%= render "form" %>
    </div>
  </div>
</section>

